import { TextDefault, ViewCenter } from "../style";

export default function Alert({ text, onConfirm, onCancel }) {
  return (
    <ViewCenter>
      <TextDefault>Alerta</TextDefault>
    </ViewCenter>
  );
}
